#include <stdio.h>

int main() {
    int number;
    int last_digit;
    printf("Enter an integer: ");
    scanf("%d", &number);  
    last_digit = number - (number / 10) * 10;
    printf("The last digit of %d is %d\n", number, last_digit);

    return 0;
}
